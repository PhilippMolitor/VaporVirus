using GameState;

namespace UI
{
    public class GameOverMenuController : StateManagedGameObject
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        public override void OnStateChange(State previous, State next)
        {
        }
    }
}
